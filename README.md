# BRIDGER
Demographic-conditioned facial expression recognition on RAF-DB: recovers race/age decodability lost during POSTER-Var fine-tuning via an auxiliary objective, then closes group Neutral-recall gaps with a shrinkage-conditioned head.
